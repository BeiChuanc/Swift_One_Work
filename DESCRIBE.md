# 仪式感布置分享

仪式感布置分享（Epoch）产品功能优化描述
一、首页功能（优先执行项 - 1）
   围绕 “仪式感布置分享” 核心主题，打造逻辑闭环、体验流畅的首页，具体要求如下：
   功能闭环：所有新增功能需覆盖完整使用流程，无 “待实现” 断点，确保用户从功能触发到完成操作的全链路可用；
   设计风格：采用新颖、创意化的现代化设计，贴合情绪释放的产品调性，兼具视觉吸引力与易用性；
   视觉配色：选用质感高级、灵动且适配情绪主题的配色方案，兼顾色彩的情绪引导性与视觉舒适度；
   交互体验：融入丰富、平滑且细腻的动画效果，如页面转场、元素交互反馈等，提升操作的沉浸感与流畅度；
   资源占位：页面中图片、图标统一使用系统原生图标占位，保证视觉统一性。

二、核心页面 UI 优化（优先执行项 - 2, 在优先执行项 - 1执行完之后执行）
所有页面UI补全、优化必须基于现代化、趣味化、界面细腻，丰富，有质感、配色要合理，创意化设计原则补全 UI

（一）我的页面
数据展示：在 “我的信息” 模块下，新增发布帖子 / 收藏帖子数据展示模块；
操作按钮：帖子展示项右上角统一添加 “举报 / 删除” 按钮（复用 举报/删除助手类）。

（二）用户中心页面
聚焦交互体验与功能完整性，补全 UI 并满足设计要求：
核心按钮：
新增 “关注” 按钮、“消息” 按钮；
点击 “消息”，按钮触发弹窗逻辑：若未关注该用户，弹窗提示需先关注；若已关注，模态弹窗展示该用户核心信息（头像、昵称、简介等），确认后进入聊天页面；
页面右上角添加 “举报” 按钮（举报 / 删除助手类）；
帖子操作：展示的帖子项右上角添加 “举报 / 删除” 按钮（复用 举报/删除助手类）。

（三）修改用户信息页面
优化信息编辑体验，补全 UI 并实现以下功能：
编辑项：支持修改用户头像（从相册选取图片）、用户名、用户简介，新增 “确认修改” 按钮；
组件复用：头像展示统一使用 “用户头像组件”；
数据逻辑：默认填充当前登录用户数据；确认修改前校验登录状态，未登录禁止操作前往登录页；若未修改数据，提交后仍保留原有信息。

（四）设置页面
极简且功能清晰的设置页设计，补全 UI 并实现：
功能按钮：展示 “用户协议（terms）”“隐私政策（privacy）”“登出”“删除账号” 四个核心按钮；
协议展示：点击 “terms/privacy” 按钮，调用协议助手类组件，加载 Assets 目录下对应协议图片展示。

（五）登录页
轻量化、高体验的登录页设计，补全 UI 并实现：
核心元素：包含用户名输入框、密码输入框、“无账号去注册” 引导文案、“登入” 按钮、Apple 登录按钮（复用 Apple 登录按钮组件）、协议文案（复用 协议助手类）、顶部右上角关闭按钮；
校验逻辑：登录前校验用户名 / 密码是否为空，为空则禁用登录操作，无需创建本地账户、查询和账号密码校验；
登录方法：仅调用用户 ViewModel 中 “通过用户 ID 登录” 的方法。

（六）注册页
简洁合规的注册页设计，补全 UI 并实现：
核心元素：包含用户名输入框、密码输入框、确认密码输入框、“注册” 按钮、协议文案（复用 协议助手类）、顶部左上角返回按钮；
校验逻辑：注册前校验用户名 / 密码是否为空、两次密码是否一致，任一条件不满足则禁用注册操作，无需创建本地账户、查询和账号密码校验；
登录关联：注册完成后，仅调用用户 ViewModel 中 “通过用户 ID 登录” 的方法。

（七）消息列表页面
响应式、沉浸式的消息列表设计，补全 UI 并实现：
数据展示：基于消息 ViewModel 的数据，响应式展示当前登录用户有聊天记录的所有用户列表，适配不同屏幕尺寸；
设计风格：符合现代化、趣味化、创意化要求，提升列表浏览与交互体验。

（八）与用户聊天界面
场景化的聊天交互设计，补全 UI 并实现：
顶部区域：
居中卡片式展示当前聊天用户的头像、昵称、简介，点击卡片跳转至该用户中心（用户中心隐藏 “消息” 按钮，“关注” 按钮居中展示）；
右上角添加 “举报” 按钮（复用 举报/删除助手类组件）；
底部区域：包含输入框（带发送按钮）、视频通话按钮；
联动逻辑：若从聊天 / 视频通话页面进入用户中心并取消关注该用户，自动返回消息列表；若与该用户存在聊天记录，同步移除该条对话记录。

（九）发布页
便捷化的内容发布设计，补全 UI 并实现：
发布元素：包含标题输入框、内容输入框、媒体上传入口（从相册选取单张图片 / 单个视频）、“确认发布” 按钮、带下划线的 “EULA” 按钮（位于确认发布按钮下方 10px 处）；
发布校验：
优先校验登录状态，未登录则跳转至登录页；
校验标题、内容、媒体是否为空，任一为空则给出明确提示；
发布后处理：发布成功后自动清空页面所有输入 / 选择数据，恢复初始状态。

（十）帖子详情页
沉浸式的内容详情设计，补全 UI 并实现：
帖子展示：完整展示帖子内容，登录用户视角下，页面右上角添加 “举报 / 删除” 按钮（复用 举报/删除助手类）；
媒体展示：展示帖子中的图片 / 视频（复用 媒体展示组件）；
评论交互：
展示评论列表，每条评论项右上角添加 “举报” 按钮（复用 举报/删除助手类组件）；
举报单条评论后，自动删除该条评论；
数据响应：页面数据（如评论数、帖子状态等）发生变动时，自动实时刷新展示。

（十一）发现页
个性化的内容浏览设计，补全 UI 并实现：
布局展示：采用非规则瀑布流布局展示帖子，提升视觉层次感与浏览趣味性；
帖子项展示：
右上角添加 “举报 / 删除” 按钮（复用 举报/删除助手类组件）；
展示维度：发布者信息（头像复用 “用户头像组件”、发布者昵称）、帖子标题、帖子核心内容、帖子媒体（媒体展示组件）；
设计风格：符合现代化、趣味化、创意化要求，强化内容发现的体验感。
